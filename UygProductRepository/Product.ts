export class Product
{
    constructor(public id?: number, public name?: string, public category?: string, public price?: number) //Soru işaretleri sayesinde null gelebilir dedik.
    {
        
    }
}